<h2>인증메일을 보낼 서강대 메일을 적어주세요</h2>
<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>

  <div class="field">
    <p><%= f.label :아이디 %><br />
    <%= f.text_field :username, autofocus: true %></p>
  </div>

  <div class="actions">
    <%= f.submit "인증메일 전송" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
