<h1>글이랑 썸네일 보여주기 + 글 쓰러가기 링크</h1>
<a href = "/home/write"> 글 써볼래?</a>

<div class="container">
    <% @posts.each do |p| %>
      <p><%= p.title %></p>
      <p><%= p.content %></p>
    <% end %>
</div>